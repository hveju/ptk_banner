{
  "name": "banner-helper",
  "version": "0.1.0",
  "description": "배너 디자인 효율화를 위한 피그마 플러그인",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.127.0",
    "typescript": "^6.0.3"
  }
}
